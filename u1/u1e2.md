# Tema 3. JavaScript profesional en una aplicación web

## Unidad 1. Trabajo avanzado con el DOM

## Ejercicio 3.1.2. (Castellano)

En el fichero `u1e2.html` podrás comprobar que se carga el script `u1e2.js` que tiene como objetivo practicar la manipulación del DOM desde JavaScript a través de métodos avanzados.

Para ello verás que cuentas con una lista de categorías en el array `CATEGORY_LIST` y una lista de proyectos en el array `PROJECT_LIST` en ese mismo fichero js.

Se pide lo siguiente:


### A. Template `tpl-project`

Crear un template con id `tpl-project` dentro del fichero html que contenga esta estructura HTML en su interior:

```html
<div class="js-project" data-id="" data-tags="" data-search="" data-archived="">
    <h2><span class="js-name">[*project_name*]</span> (<span class="js-progress">[*progress*]</span>%)</h2>
    <p class="js-excerpt">[*project_excerpt*]</p>
    <div>
        <span class="js-category">[*category_name*]</span>
        <span class="js-tags">[*tag_list*]</span>
    </div>
</div>
```


### B. Template `tpl-tag`

Crear un template con id `tpl-tag` dentro del fichero html que contenga esta estructura HTML en su interior:

```html
<span class="js-tag">
    <a class="js-tag-link" data-tag="[*tag*]" href="">[*tag*]</a>
</span>
```


### C. Función `renderProjects`

Crea una función denominada `renderProjects` que a partir de la lista de proyectos disponible genere un nodo a partir del anterior template por cada proyecto rellenando sus respectivos nodos con este criterio.

> 💡 NOTA: Te recomendamos usar `createDocumentFragment` para mejorar el rendimiento de las operaciones de manipulación del DOM.

Esta lista de proyectos se generará dentro del div con clase `js-project-list`, cumpliendo las siguientes condiciones:

- `data-id`: atributo con el id del proyecto (campo `id`).
- `data-tags`: atributo cadena de texto separada por comas con las **tags** del proyecto (campo `tags`)
- `data-search`: atributo cadena de texto separada por comas con las palabras de búsqueda del proyecto (campo `search`)
- `data-archived`: atributo con valor `true` o `false` dependiendo del valor del campo `archived` del proyecto.
- `js-name`: clase para poder manipular el nodo que contendrá el nombre del proyecto (campo `name`)
- `js-progress`: clase para poder manipular el nodo que contendrá el indicador de progreso del proyecto (campo `progress`)
- `js-excerpt`: clase para poder manipular el nodo que contendrá el texto resumen del proyecto, puede contener HTML en su interior, y ha de conservarlo.
- `js-category`: clase para poder manipular el nodo que contendrá el nombre de la categoría asociada al proyecto (campo `name` de la categoría con el id indicado en `categoryId`)
- `js-tags`: clase para poder manipular la lista de **tags**. Por cada tag, se generará un nodo tag a partir de la plantilla `tpl-tag`


Adicionalmente:

- Si el proyecto está archivado (campo `archived`) se le añadirá la clase `archived` al `div.js-project` correspondiente para que visualmente se pueda diferenciar.
- Si el proyecto está completado se la añadirá la clase `completed` al `div.js-project` correspondiente para que visualmente se pueda diferenciar.


### D. Llamada inicial a la función

Escribe la llamada a la función `renderProjects` para que genere la lista de proyectos en el fichero html inicialmente.


-----


# Tema 3. JavaScript professional en una aplicació web

## Unitat 1. Treball avançat amb el DOM

## Exercici 3.1.2. (Català)

Al fitxer `u1e2.html` podràs comprovar que es carrega com a mòdul l'script `u1e2.js` que té com a objectiu practicar la manipulació del DOM des de JavaScript a través de mètodes avançats.

Per això veuràs que comptes amb una llista de categories a l'array `CATEGORY_LIST` i una llista de projectes a l'array `PROJECT_LIST` en aquest mateix fitxer js.

Es demana el següent:


### A. Template `tpl-project`

Crear un template amb id `tpl-project` dins del fitxer html que contingui aquesta estructura HTML al seu interior:

```html
<div class="js-project" data-id="" data-tags="" data-search="" data-archived="">
     <h2><span class="js-name">[*project_name*]</span> (<span class="js-progress">[*progress*]</span>%)</h2>
     <p class="js-excerpt">[*project_excerpt*]</p>
     <div>
         <span class="js-category">[*category_name*]</span>
         <span class="js-tags">[*tag_list*]</span>
     </div>
</div>
```


### B. Template `tpl-tag`

Crear un template amb id `tpl-tag` dins del fitxer html que contingui aquesta estructura HTML al seu interior:

```html
<span class="js-tag">
     <a class="js-tag-link" data-tag="[*tag*]" href="">[*tag*]</a>
</span>
```


### C. Funció `renderProjects`

Crea una funció anomenada `renderProjects` que a partir de la llista de projectes disponible generi un node a partir de l'anterior template per cada projecte omplint els seus respectius nodes amb aquest criteri.

> 💡 NOTA: Et recomanem utilitzar `createDocumentFragment` per millorar el rendiment de les operacions de manipulació del DOM.

Aquesta llista de projectes es generarà dins del div amb classe `js-project-list`, complint les condicions següents:

- `data-id`: atribut amb l'id del projecte (camp `id`).
- `data-tags`: atribut cadena de text separada per comes amb les **tags** del projecte (camp `tags`)
- `data-search`: atribut cadena de text separada per comes amb les paraules de cerca del projecte (camp `search`)
- `data-archived`: atribut amb valor `true` o `false` depenent del valor del camp `archived` del projecte.
- `js-name`: classe per poder manipular el node que contindrà el nom del projecte (camp `name`)
- `js-progress`: classe per poder manipular el node que contindrà l'indicador de progrés del projecte (camp `progress`)
- `js-excerpt`: classe per poder manipular el node que contindrà el text resum del projecte, pot contenir HTML a dins, i ha de conservar-lo.
- `js-category`: classe per poder manipular el node que contindrà el nom de la categoria associada al projecte (camp `name` de la categoria amb l'id indicat a `categoryId`)
- `js-tags`: classe per poder manipular la llista de **tags**. Per cada tag, es generarà un node tag a partir de la plantilla `tpl-tag`


Addicionalment:

- Si el projecte està arxivat (camp `archived`) s'afegirà la classe `archived` al `div.js-project` corresponent perquè visualment es pugui diferenciar.
- Si el projecte està completat s'hi afegirà la classe `completed` al `div.js-project` corresponent perquè visualment es pugui diferenciar.


### D. Crida inicial a la funció.

Escriu la crida a la funció `renderProjects` perquè generi la llista de projectes al fitxer html inicialment.
